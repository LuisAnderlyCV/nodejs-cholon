<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" type="image/x-icon" href="/src/views/img/icono.ico">
    <title>CLIENTES</title>

    <!-- Custom fonts for this template-->
    <link href="/src/boostrapdiseñoadmin/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link
        href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i"
        rel="stylesheet">

    <!-- Custom styles for this template-->
    <link href="/src/boostrapdiseñoadmin/css/sb-admin-2.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/src/views/css/registrar.css">
    <link rel="stylesheet" href="/src/views/css/estiloperfiles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"
        integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body id="page-top">

    <!-- Page Wrapper -->
    <div id="wrapper">

        <!-- Sidebar -->
        <%- include('../plantilla/listamodulos') %>
            <!-- End of Sidebar -->

            <!-- Content Wrapper -->
            <div id="content-wrapper" class="d-flex flex-column">

                <!-- Main Content -->
                <div id="content">

                    <!-- Topbar -->
                    <%- include('../plantilla/navbar') %>
                        <!-- End of Topbar -->

                        <!-- ACA DEBEMOS COPIAR LAS TABLAS O REGISTROS PARTE BLANCA -->
                        <div class="container-fluid overflow-auto">
                            <h1></h1>

                            <div class="row g-3">
                                <div class="col">
                                    <button type="button" class="btn btn-primary" data-bs-toggle="modal"
                                        data-bs-target="#exampleModal1">
                                        AGREGAR CLIENTES
                                    </button>
                                </div>
                                <div class="col">
                                    <input type="text" class="form-control" id="busqueda"
                                        placeholder="  🔍︎    Buscar cliente..."
                                        oninput="this.value = this.value.toUpperCase()">
                                </div>
                            </div>


                            <h1>CLIENTES EN EL SISTEMA</h1>
                            <table class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th>ID de Cliente</th>
                                        <th>Documento</th>
                                        <th>Cliente</th>
                                        <th>Telefono</th>
                                        <th>Direccion</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% clientes.forEach(function(cliente) { %>
                                        <tr>
                                            <td>
                                                <%= cliente.idcliente %>
                                            </td>
                                            <td>
                                                <%= cliente.rucydni %>
                                            </td>
                                            <td>
                                                <%= cliente.nombre%>
                                            </td>
                                            <td>
                                                <%= cliente.telefono %>
                                            </td>
                                            <td>
                                                <%= cliente.direccion %>
                                            </td>
                                            <td>
                                                <button onclick="botoneditar(<%=  cliente.idcliente %>)"
                                                    type="button" class="btn btn-primary" data-bs-toggle="modal"
                                                    data-bs-target="#exampleModal">
                                                    <i class="bi bi-pencil-square"></i>
                                                </button>

                                                <button onclick="botoneliminar(<%=  cliente.idcliente %>) "
                                                    type="button" class="btn btn-danger"><i
                                                        class="bi bi-trash-fill"></i></button>
                                            </td>
                                        </tr>
                                        <% }); %>
                                </tbody>
                            </table>
                            <h1>CLIENTES ELIMINADOS</h1>
                            <table class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th>ID de Cliente</th>
                                        <th>Documento</th>
                                        <th>Cliente </th> 
                                        <th>Telefono</th>
                                        <th>Direccion</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <%  clientesInactivos.forEach(function( cliente) { %>
                                        <tr>
                                            <td>
                                                <%= cliente.idcliente %>
                                            </td>
                                            <td>
                                                <%=  cliente.rucydni %>
                                            </td>
                                            <td>
                                                <%=  cliente.nombre%>
                                            </td>
                                            <td>
                                                <%=  cliente.telefono %>
                                            </td>
                                            <td>
                                                <%=  cliente.direccion %>
                                            </td>
                                            <td>

                                                <button onclick="botonveract(<%= cliente.idcliente %>) "
                                                    type="button" class="btn btn-success">RESTABLECER</button>
                                            </td>
                                        </tr>
                                        <% }); %>
                                </tbody>
                            </table>
                            <!-- MODAL PARA AGREGAR PROVEEDOR-->
                            <div class="modal fade" id="exampleModal1" tabindex="-1" aria-labelledby="exampleModalLabel"
                                aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h1 class="modal-title fs-5" id="exampleModalLabel">AGREGAR CLIENTE</h1>

                                        </div>
                                        <div class="modal-body">
                                            <div class="right-container" style="padding: 20px; flex-grow: 1;">
                                                <form id="myForm">
                                                    <label for="ruc">BUSCAR CON:</label>
                                                    <div class="form-check">
                                                        <input class="form-check-input" type="radio"
                                                            name="flexRadioDefault" id="rucradio" checked>
                                                        <label class="form-check-label" for="flexRadioDefault1">
                                                            RUC
                                                        </label>
                                                    </div>
                                                    <div class="form-check">
                                                        <input class="form-check-input" type="radio"
                                                            name="flexRadioDefault" id="dniradio">
                                                        <label class="form-check-label" for="flexRadioDefault2">
                                                            DNI
                                                        </label>
                                                    </div>
                                                    <div class="input-group mb-3">
                                                        <input type="text" class="form-control" id="rucydni"
                                                            oninput="limitarADigitosruc(this)" pattern="[0-9]*">
                                                        <a class="btn btn-primary" id="buscarprove"
                                                            style="width: 3em; font-size: 1em;"><i
                                                                class="bi bi-search"></i></a>
                                                    </div>
                                                    <label for="nombre">Cliente:</label>
                                                    <input type="text" placeholder="Ingrese nombre del Cliente"
                                                        id="nombre"
                                                        oninput="this.value = this.value.toUpperCase()">


                                                    <label for="telefono">Telefono:</label>
                                                    <input type="text" placeholder="Ingrese telefono" id="telefono"
                                                        oninput="limitarADigitos(this, 9)" pattern="[0-9]*">
                                                        
                                                    <label for="direccion">Direccion:</label>
                                                    <input type="text" placeholder="Ingrese Direccion" id="direccion"
                                                    oninput="validarYConvertir1(this)">
                                                    

                                                </form>
                                            </div>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary"
                                                data-bs-dismiss="modal">Close</button>
                                            <button type="button" class="btn btn-primary"
                                                onclick="postDatos()">Agregar</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <!-- MODAL PARA EDITAR-->
                            <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel"
                                aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h1 class="modal-title fs-5" id="exampleModalLabel">ACTUALIZAR CLIENTE
                                            </h1>

                                        </div>
                                        <div class="modal-body">
                                            <div class="right-container" style="padding: 20px; flex-grow: 1;">
                                                <form id="myForm">
                                                    <label for="idcliente">ID:</label>
                                                    <input type="text" placeholder="Ingrese su ID" id="idcliente"
                                                        readonly>

                                                    <label for="rucydni1">RUC:</label>
                                                    <input type="text" placeholder="Ingrese su RUC" id="rucydni1" readonly
                                                        oninput="limitarADigitos(this, 11)" pattern="[0-9]*">

                                                        <label for="nombre1">Nombre:</label>
                                                        <input type="text" placeholder="Ingrese Nombre" id="nombre1"
                                                            oninput="this.value = this.value.toUpperCase()">

                                                    <label for="telefono1">Telefono:</label>
                                                    <input type="text" placeholder="Ingrese Telefono" id="telefono1"
                                                        oninput="validarYConvertir(this)">

                                                    <label for="direccion1">Direccion:</label>
                                                    <input type="text" placeholder="Ingrese Direccion" id="direccion1"
                                                        oninput="validarYConvertir1(this)">

                                                    


                                                    
                                                </form>
                                            </div>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary"
                                                data-bs-dismiss="modal">Close</button>
                                            <button type="button" class="btn btn-primary"
                                                onclick="guardarDatos()">Actualizar</button>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        </div>
                        <footer class="sticky-footer bg-white">
                            <div class="container my-auto">
                                <div class="copyright text-center my-auto">
                                    <span>Copyright &copy; UNSM 2023</span>
                                </div>
                            </div>
                        </footer>
                </div>
                <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
                <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.all.min.js"></script>


                <script>


                    function postDatos() {
                        // Obtener los valores de los campos de entrada
                        const rucydni = document.getElementById('rucydni').value;
                        const nombre = document.getElementById('nombre').value;
                        const telefono = document.getElementById('telefono').value;
                        const direccion = document.getElementById('direccion').value;

                        // Crear un objeto de datos con los valores para usar en los controolers
                        const data = {
                            rucydni: rucydni,
                            nombre: nombre,
                            telefono: telefono,
                            direccion: direccion,
                        };

                        // Enviar una solicitud POST a la ruta "clientes" utilizando Axios
                        axios.post('/admin/clientes', data)
                            .then(function (response) {
                                // Mostrar un mensaje de éxito con SweetAlert
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Registro exitoso',
                                    text: 'El Cliente se realizó correctamente.',
                                    timer: 2000, // Cambia la duración del mensaje si lo deseas
                                    showConfirmButton: false // Oculta el botón "OK"
                                });

                                // Redirigir al usuario después de 2 segundos (2000 ms)
                                setTimeout(function () {
                                    window.location.href = '/admin/clientes';
                                }, 2000);
                            })

                    }


                    function botoneditar(idcliente) {
                        // Asignar el ID del perfil al campo oculto

                        axios.get(`/admin/clientes/${idcliente}`)
                            .then(function (response) {
                                const respuestadelquery = response.data;
                                if (respuestadelquery) {
                                    console.log(respuestadelquery); // Imprimir los datos en la consola
                                    document.getElementById('idcliente').value = respuestadelquery.idcliente;
                                    document.getElementById('rucydni1').value = respuestadelquery.rucydni;
                                    document.getElementById('nombre1').value = respuestadelquery.nombre;
                                    document.getElementById('telefono1').value = respuestadelquery.telefono;
                                    document.getElementById('direccion1').value = respuestadelquery.direccion;


                                }
                            })
                            .catch(function (error) {
                                console.error(error);
                            });
                    }
//
function validarYConvertir1(input) {
    // Expresión regular que acepta solo letras mayúsculas, números y espacios
    var regex = /^[A-Z0-9\s]+$/;

    // Convierte a mayúsculas y elimina caracteres no permitidos
    input.value = input.value.toUpperCase().replace(/[^A-Z0-9\s]/g, '');
}
//MAYUSCULA Y NUMERO SIN ESPACIO
function validarYConvertir2(input) {
    // Expresión regular que acepta solo letras mayúsculas y números
    var regex = /^[A-Z0-9]+$/;

    // Convierte a mayúsculas y elimina caracteres no permitidos
    input.value = input.value.toUpperCase().replace(/[^A-Z0-9]/g, '');
}

                    function guardarDatos() {
                        // Obtén el valor del campo de entrada
                        // Obtener los valores de los campos de entrada
                        const idcliente = document.getElementById('idcliente').value;
                        const rucydni1 = document.getElementById('rucydni1').value;
                        const nombre1 = document.getElementById('nombre1').value;
                        const telefono1 = document.getElementById('telefono1').value;
                        const direccion1 = document.getElementById('direccion1').value;

                        // Crear un objeto de datos con los valores para usar en los controolers
                        const data = {
                            rucydni1: rucydni1,
                            nombre1: nombre1,
                            telefono1: telefono1,
                            direccion1: direccion1,
                        };

                        console.log(data);


                        axios.put(`/admin/clientes/${idcliente}`, data)
                            .then(function (response) {
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Registro exitoso',
                                    text: 'El Proveedor se realizó correctamente.',
                                    timer: 2000,
                                    showConfirmButton: false
                                }).then(function () {
                                    // Después de que el usuario haya visto la notificación, actualiza la página
                                    location.reload(); // Esto recargará la página actual
                                });
                            })
                            .catch(function (error) {
                                console.error(error);
                            });

                    }


                    function botoneliminar(idcliente) {
                        // Utiliza SweetAlert2 para mostrar una ventana modal de confirmación
                        Swal.fire({
                            title: '¿Estás seguro?',
                            text: 'Esta acción eliminará al Cliente con ID ' + idcliente,
                            icon: 'warning',
                            showCancelButton: true,
                            confirmButtonText: 'Sí, eliminar',
                            cancelButtonText: 'Cancelar'
                        }).then((result) => {
                            if (result.isConfirmed) {
                                // El usuario confirmó la eliminación, realiza la solicitud DELETE con Axios
                                axios.delete(`/admin/clientes/${idcliente}`)
                                    .then((response) => {
                                        // Si la solicitud DELETE se completa con éxito, puedes manejar la respuesta aquí
                                        console.log('Cliente eliminado con éxito');
                                        // Puedes realizar acciones adicionales, como redirigir a otra página o actualizar la interfaz de usuario
                                        window.location.reload();
                                    })
                                    .catch((error) => {
                                        // Si hay un error en la solicitud DELETE, puedes manejarlo aquí
                                        console.error('Error al eliminar al Cliente:', error);
                                        // Puedes mostrar un mensaje de error o realizar otras acciones según sea necesario
                                    });
                            }
                        });
                    }

                    function botonveract(idcliente) {
                        // Utiliza SweetAlert2 para mostrar una ventana modal de confirmación
                        Swal.fire({
                            title: '¿Estás seguro?',
                            text: 'Esta acción restablecera al Cliente con ID ' + idcliente,
                            icon: 'warning',
                            showCancelButton: true,
                            confirmButtonText: 'Sí, restablecer',
                            cancelButtonText: 'Cancelar'
                        }).then((result) => {
                            if (result.isConfirmed) {
                                // El usuario confirmó la eliminación, realiza la solicitud DELETE con Axios
                                axios.delete(`/admin/borracliente/${idcliente}`)
                                    .then((response) => {
                                        // Si la solicitud DELETE se completa con éxito, puedes manejar la respuesta aquí
                                        console.log('Cliente restablecido con éxito');
                                        // Puedes realizar acciones adicionales, como redirigir a otra página o actualizar la interfaz de usuario
                                        window.location.reload();
                                    })
                                    .catch((error) => {
                                        // Si hay un error en la solicitud DELETE, puedes manejarlo aquí
                                        console.error('Error al restablecer Cliente:', error);
                                        // Puedes mostrar un mensaje de error o realizar otras acciones según sea necesario
                                    });
                            }
                        });
                    }


                    //------------ESTO ES PARA ELEGIR QUE TIPO DE DOCUMENTO
                    // Function to limit the number of digits based on the selected radio button
                    function limitarADigitosruc(input) {
                        var maxLength = 11; // Default for RUC
                        if (document.getElementById("dniradio").checked) {
                            maxLength = 8; // Change to 8 if DNI is selected
                        }
                        input.value = input.value.slice(0, maxLength);
                    }

                    // Event listeners for radio buttons
                    document.getElementById("rucradio").addEventListener("change", function () {
                        limitarADigitos(document.getElementById("rucydni"));
                        if (this.checked) {
                            handleRUC();
                        }
                        document.getElementById('rucydni').value = "";
                    });

                    document.getElementById("dniradio").addEventListener("change", function () {
                        limitarADigitos(document.getElementById("rucydni"));
                        if (this.checked) {
                            handleDNI();
                        }
                        document.getElementById('rucydni').value = "";
                    });

                    // Function to handle RUC selection
                    function handleRUC() {
                        alert("Selected RUC");
                    }

                    // Function to handle DNI selection
                    function handleDNI() {
                        alert("Selected DNI");
                    }

                    //------------------------------------------------------


                    // CONSULTA A LA API DE SUNAT
                    const botonproveedor = document.getElementById("buscarprove");

                    // Asignar el evento click al botón
                    botonproveedor.addEventListener("click", () => {
                        if (document.getElementById("rucradio").checked) {
                            obtenerDatosProveedor();
                        }
                        else {
                            obtenerDatosProveedordni();
                        }

                    });

                    async function obtenerDatosProveedor() {
                        const rucProveedor = document.getElementById("rucydni").value;
                        const nombreInput = document.getElementById("nombre");
                        const direccionInput = document.getElementById('direccion');

                        if (rucProveedor.length === 11) {
                            const apiUrl = `https://dniruc.apisperu.com/api/v1/ruc/${rucProveedor}?token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJlbWFpbCI6ImhsZmVybmFuZGV6ckBhbHVtbm8udW5zbS5lZHUucGUifQ.lJpdzo238Z3HdZVTfwqp46EAT08Q_V9MLCiTBUy__O4`;

                            try {
                                const response = await fetch(apiUrl);
                                const data = await response.json();
                                console.log(data);
                                const nombreProveedorxml = data.razonSocial;
                                const direccionxml = data.direccion;

                                nombreInput.value = nombreProveedorxml;
                                direccionInput.value = direccionxml;

                                console.log(data);
                            } catch (error) {
                                console.log("Error:", error);
                            }
                        } else if (rucProveedor.length <= 10) {
                            nombreInput.value = "";
                            direccionInput.value = "";
                            ciudadInput.value = "";
                        }
                    }

                    async function obtenerDatosProveedordni() {
                        const rucProveedor = document.getElementById("rucydni").value;
                        const nombreInput = document.getElementById("nombre");

                        if (rucProveedor.length === 8) {
                            const apiUrl = `https://dniruc.apisperu.com/api/v1/dni/${rucProveedor}?token=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJlbWFpbCI6ImhsZmVybmFuZGV6ckBhbHVtbm8udW5zbS5lZHUucGUifQ.lJpdzo238Z3HdZVTfwqp46EAT08Q_V9MLCiTBUy__O4`;

                            try {
                                const response = await fetch(apiUrl);
                                const data = await response.json();
                                console.log(data);
                                const nombreProveedorxml = data.nombres + " " + data.apellidoPaterno + " " + data.apellidoMaterno;
                                

                                nombreInput.value = nombreProveedorxml;
                                

                                console.log(data);
                            } catch (error) {
                                console.log("Error:", error);
                            }
                        } else if (rucProveedor.length <= 10) {
                            nombreInput.value = "";
                            
                        }
                    }

                    //PARA BUSCAR EN MI TABLA
                    // Obtener referencia al campo de búsqueda
                    const campoBusqueda = document.getElementById('busqueda');

                    // Obtener todas las filas de la tabla
                    const filasTabla = document.querySelectorAll('tbody tr');

                    // Agregar un evento de entrada al campo de búsqueda
                    campoBusqueda.addEventListener('input', function () {
                        const consulta = campoBusqueda.value.toLowerCase();

                        // Iterar a través de las filas de la tabla y mostrar/ocultar según la consulta
                        filasTabla.forEach(function (fila) {
                            const contenidoFila = fila.textContent.toLowerCase();
                            if (contenidoFila.includes(consulta)) {
                                fila.style.display = 'table-row';
                            } else {
                                fila.style.display = 'none';
                            }
                        });
                    });
                    //PARA LIMITAR A 7 NUMEROS
                    function limitarADigitos(input, maxDigitos) {
                        // Elimina cualquier carácter no numérico
                        input.value = input.value.replace(/[^0-9]/g, '');

                        // Limita la longitud a maxDigitos
                        if (input.value.length > maxDigitos) {
                            input.value = input.value.slice(0, maxDigitos);
                        }
                    }

                    //PARA HACER QUE SEA PURA MAYUSCULA Y SIN NUMEROS
                    function validarYConvertir(input) {
                        // Expresión regular que acepta solo letras y espacios
                        var regex = /^[A-Za-z\s]+$/;

                        if (!regex.test(input.value)) {
                            // Si el valor no cumple con la expresión regular, elimina los caracteres no permitidos
                            input.value = input.value.replace(/[^A-Za-z\s]+/, '');
                        }

                        // Convierte a mayúsculas
                        input.value = input.value.toUpperCase();
                    }


                    //PARA ESCRIBIR PURA MAYUSCULA
                    //function convertirAMayusculas(input) {
                       // input.value = input.value.toUpperCase();
                    //}


                </script>
                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
                    integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
                    crossorigin="anonymous"></script>
            </div>
            <!-- /.container-fluid -->

    </div>
    <!-- End of Main Content -->

    <!-- Footer -->

    <!-- End of Footer -->

    </div>
    <!-- End of Content Wrapper -->

    </div>
    <!-- End of Page Wrapper -->

    <!-- Scroll to Top Button-->
    <a class="scroll-to-top rounded" href="#page-top">
        <i class="fas fa-angle-up"></i>
    </a>

    <!-- Logout Modal-->
    <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
                    <a class="btn btn-primary" href="login.html">Logout</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap core JavaScript-->
    <script src="/src/boostrapdiseñoadmin/vendor/jquery/jquery.min.js"></script>
    <script src="/src/boostrapdiseñoadmin/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

    <!-- Core plugin JavaScript-->
    <script src="/src/boostrapdiseñoadmin/vendor/jquery-easing/jquery.easing.min.js"></script>

    <!-- Custom scripts for all pages-->
    <script src="/src/boostrapdiseñoadmin/js/sb-admin-2.min.js"></script>

</body>

</html>